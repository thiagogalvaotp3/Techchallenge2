# Tech Challenge — Fase 2 | Classificação de qualidade de vinhos tintos (WineQT)
# Grupo: Efraim Oliveira, Érica Tarsis, Ricardo Moraes, Rodrigo Bernardino, Thiago Galvão
# POSTECH DTAT — 2026
#
# Versões de referência (mínimas) compatíveis com a stack validada (SEED = 43).
# Instalação: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
statsmodels>=0.14
scipy>=1.10
shap>=0.44          # opcional: usado só na Figura 13 (SHAP); o código ignora se ausente
notebook>=7.0       # para abrir notebooks/analise_e_modelagem.ipynb
